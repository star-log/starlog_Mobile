import 'package:flutter/material.dart';

class StarlogMain extends StatelessWidget {
  const StarlogMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.transparent,
    );
  }
}
